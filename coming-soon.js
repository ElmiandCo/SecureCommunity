(()=>{
const canvas=document.getElementById('space'),ctx=canvas.getContext('2d'),scenes=[...document.querySelectorAll('.scene')],dots=[...document.querySelectorAll('.journey-progress span')];let w=0,h=0,stars=[],scrollY=window.scrollY,target=scrollY,smooth=scrollY;
function resize(){const d=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*d;canvas.height=h*d;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(d,0,0,d,0,0);stars=Array.from({length:Math.min(240,Math.max(100,Math.floor(w*h/6500)))},()=>({x:Math.random()*w,y:Math.random()*h,z:.12+Math.random()*.88,r:.35+Math.random()*1.25,a:.25+Math.random()*.65,p:Math.random()*Math.PI*2}));}
function draw(){smooth+=(target-smooth)*.055;const delta=smooth-scrollY;scrollY+=delta;ctx.clearRect(0,0,w,h);for(const s of stars){const depth=s.z*.32;let y=(s.y-(smooth*depth)%h+h)%h;let x=(s.x+Math.sin(s.p+smooth*.00035)*s.z*10+w)%w;ctx.globalAlpha=s.a*(.35+.65*s.z);ctx.beginPath();ctx.arc(x,y,s.r*s.z+.25,0,Math.PI*2);ctx.fillStyle='#fff7d6';ctx.fill()}ctx.globalAlpha=1;const active=Math.min(scenes.length-1,Math.max(0,Math.round(smooth/Math.max(1,h*.92))));dots.forEach((d,i)=>d.classList.toggle('active',i===active));requestAnimationFrame(draw)}
window.addEventListener('scroll',()=>{target=window.scrollY},{passive:true});window.addEventListener('resize',resize);const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('in-view')),{threshold:.35});scenes.forEach(s=>io.observe(s));resize();draw();
})();
