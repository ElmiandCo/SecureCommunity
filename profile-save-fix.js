/* Retired compatibility shim.
 * The unified profile-builder.js now owns the complete save flow.
 * This file intentionally installs no click handlers so there is only one
 * authoritative profile editor and one authoritative Save action.
 */
(function(){ 'use strict'; })();
